<ol>
  <% submittable_object.options.each do |option| -%>
    <li>
      <%= submission_fields.text_field :text, option.text, id: dom_id(option) %>
      <%= content_tag :label, option.text, for: dom_id(option) %>
    </li>
  <% end -%>
</ol>
